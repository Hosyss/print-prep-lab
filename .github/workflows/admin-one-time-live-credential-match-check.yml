name: Retired one-time live credential diagnostic

on:
  push:
    branches:
      - __retired_never__

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This one-time diagnostic is retired."
